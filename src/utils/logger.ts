/**
 * LogInfo is a function that takes a string and logs it to the console
 * @param {string} message - string - This is the message that will be logged to the console.
 */
export const LogInfo = (message: string) => {
  console.log(`Success ${message}`)
}

/**
 * LogSuccess is a function that takes a string and logs it to the console
 * @param {string} message - string - This is the message that will be logged to the console.
 */
export const LogSuccess = (message: string) => {
  console.log(`Success ${message}`)
}

/**
 * LogWarning takes a string and logs it to the console
 * @param {string} message - string - The message to log
 */
export const LogWarning = (message: string) => {
  console.log(`Success ${message}`)
}

/**
 * LogError is a function that takes a string and logs it to the console
 * @param {string} message - string - The message to log
 */
export const LogError = (message: string) => {
  console.log(`Success ${message}`)
}
